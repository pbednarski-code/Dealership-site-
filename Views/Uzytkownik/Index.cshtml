@model IEnumerable<DealerAutoMVC.Models.Uzytkownik>

@{
    ViewData["Title"] = "Użytkownicy";
}

<h1>Użytkownicy</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Login)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HasloHash)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TokenApi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CzyAdmin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Login)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HasloHash)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TokenApi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CzyAdmin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
